# CalendarTableTH

A calendar table interactive grid header cell for managing dates, days, etc. <th>.

See `components/calendar-table-th/index.md` for canonical documentation.

## Parameters

- `Label`: string (optional) — accessible label set on `aria-label`
- `CssClass`: string — extra CSS classes appended to `calendar-table-th`
- `ChildContent`: RenderFragment — component content
- `AdditionalAttributes`: catches unmatched HTML attributes

## Usage

```razor
<CalendarTableTH>
    Content
</CalendarTableTH>
```
