# DataTableHead

A data table interactive grid thead for displaying and sorting tabular data <thead>.

See `components/data-table-head/index.md` for canonical documentation.

## Parameters

- `Label`: string (optional) — accessible label set on `aria-label`
- `CssClass`: string — extra CSS classes appended to `data-table-head`
- `ChildContent`: RenderFragment — component content
- `AdditionalAttributes`: catches unmatched HTML attributes

## Usage

```razor
<DataTableHead>
    Content
</DataTableHead>
```
