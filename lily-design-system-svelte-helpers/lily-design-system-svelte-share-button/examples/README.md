# Examples — ShareButton

| File | Shows |
| ---- | ----- |
| [basic.svelte](./basic.svelte) | Consumer-supplied destinations, the built-in copy item, and the announced copy outcome. |

Every user-facing string is a prop, including `copyLabel` — the copy item
renders only when you name it, because a default would be hardcoded
English.
