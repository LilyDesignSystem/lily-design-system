<!-- Replace `<html lang="en">` in your project's src/app.html with this so
     the data-text-size attribute is in place before any CSS evaluates. The
     %textSize% placeholder is filled in by hooks.server.ts. -->
<html lang="en" data-text-size="%textSize%">
