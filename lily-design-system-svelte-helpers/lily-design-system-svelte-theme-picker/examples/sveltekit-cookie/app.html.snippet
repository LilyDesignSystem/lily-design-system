<!-- Replace `<html lang="en">` in your project's src/app.html with this so
     the data-theme attribute is in place before any CSS evaluates. The
     %theme% placeholder is filled in by hooks.server.ts. -->
<html lang="en" data-theme="%theme%">
